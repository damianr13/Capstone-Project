<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ValueText" parent="TextAppearance.AppCompat">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="LabelText" parent="ValueText">
        <item name="android:layout_marginTop">@dimen/margin_top_default</item>
        <item name="android:textColor">#888888</item>
    </style>

    <style name="BottomBarImageAction">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">@dimen/margin_start_default</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="ToolbarStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:background">?attr/colorPrimary</item>
    </style>

    <style name="CircleProfilePicture">
        <item name="android:layout_width">@dimen/circle_image_size</item>
        <item name="android:layout_height">@dimen/circle_image_size</item>
        <item name="android:layout_marginTop">@dimen/margin_top_default</item>
        <item name="android:layout_marginStart">@dimen/margin_start_default</item>
    </style>
</resources>
